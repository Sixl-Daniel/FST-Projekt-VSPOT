@if(!empty($screen->heading))
    <h1 class="heading heading--main">{{ $screen->heading }}</h1>
@endif
@if(!empty($screen->subheading))
    <h3 class="heading heading--sub">{{ $screen->subheading }}</h3>
@endif
